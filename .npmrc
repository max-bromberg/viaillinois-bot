# Vite 8 declares esbuild as an optional peer dependency, and Vitest does not
# need it. The lock file was written without it, so every install, including
# npm ci in the gate and in the container image, resolves the lock the same way.
legacy-peer-deps=true
